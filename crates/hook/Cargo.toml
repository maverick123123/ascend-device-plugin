[package]
name = "hook"
version.workspace = true
edition.workspace = true

[lib]
name = "vnpu"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
lazy_static = { workspace = true }
libc = { workspace = true }
limiter = { path = "../limiter" }
once_cell = "1.18"
log = "0.4"
